{
  "name": "mooncake-explorer",
  "private": true,
  "version": "0.1.0",
  "description": "Interactive explorer for Mooncake's reverse-mode AD IR",
  "scripts": {
    "setup": "julia --project=MooncakeTracer -e 'using Pkg; Pkg.instantiate()' && npm install --prefix web",
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "bake": "julia --project=MooncakeTracer -e 'using MooncakeTracer; MooncakeTracer.bake()'",
    "test": "julia --project=MooncakeTracer -e 'using Pkg; Pkg.test()'"
  }
}
